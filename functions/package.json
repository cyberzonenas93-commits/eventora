{
  "name": "eventora-functions",
  "description": "Cloud Functions for Eventora notifications and campaigns.",
  "private": true,
  "main": "index.js",
  "type": "commonjs",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "lint": "node --check index.js && node --check event_notifications.js && node --check event_payments.js && node --check organizer_applications.js",
    "test": "npm run lint"
  },
  "dependencies": {
    "firebase-admin": "^13.5.0",
    "firebase-functions": "^6.4.0"
  }
}
